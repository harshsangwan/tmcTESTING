# Docker environment configuration for integration-service
spring.application.name=integration-service
server.port=8084

# Database Configuration for Docker
spring.datasource.url=jdbc:mysql://mysql:3306/task_management_integrations?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=letmEc0de#8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# Configure connection pool
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=5
spring.datasource.hikari.idle-timeout=300000

# JWT Configuration
app.jwt.secret=${JWT_SECRET:3cfa76ef14937c1c0ea519f8fc057a80fcd04a7420f8e8bcd0a7567c272e007b80c0cb73d3d121182c95878b9a33cd8caf5bee427d6f0b9b5b989f0bd25c035}
app.jwt.issuer=taskmanagement

# Eureka Client Configuration for Docker
eureka.client.service-url.defaultZone=http://discovery-service:8761/eureka/
eureka.instance.prefer-ip-address=true
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# Disable Spring Cloud Config in Docker
spring.cloud.config.enabled=false
spring.config.import=optional:configserver:

# Feign Client Configuration
feign.client.config.default.connectTimeout=5000
feign.client.config.default.readTimeout=5000
feign.client.config.default.loggerLevel=basic

# Microservice URLs
service.auth-service.url=http://auth-service
service.task-service.url=http://task-service
service.project-service.url=http://project-service

# Integration Configuration (use environment variables in production)
integration.google.client-id=${GOOGLE_CLIENT_ID:google-client-id}
integration.google.client-secret=${GOOGLE_CLIENT_SECRET:google-client-secret}
integration.slack.client-id=${SLACK_CLIENT_ID:slack-client-id}
integration.slack.client-secret=${SLACK_CLIENT_SECRET:slack-client-secret}
integration.github.client-id=${GITHUB_CLIENT_ID:github-client-id}
integration.github.client-secret=${GITHUB_CLIENT_SECRET:github-client-secret}

# Actuator Configuration
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
management.endpoint.health.probes.enabled=true
management.health.livenessState.enabled=true
management.health.readinessState.enabled=true

# Logging
logging.level.com.taskmanagement.integration=INFO
logging.level.org.springframework.security=INFO